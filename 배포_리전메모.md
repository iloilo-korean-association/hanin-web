# 왜 `vercel.json` 에 `"regions": ["sin1"]` 이 있나

Vercel 서버리스 함수의 **기본 리전은 `iad1`(미국 워싱턴 DC)** 이다.
([공식 문서](https://vercel.com/docs/functions/configuring-functions/region) — *"By default, Vercel Functions
execute in Washington, D.C., USA (iad1) for all new projects"*)

그런데 우리 DB(Neon)는 **싱가포르(ap-southeast-1)** 에 있다.
이 설정을 빼면 이렇게 된다.

```
회원(일로일로) ──→ Vercel 함수(워싱턴 DC) ──→ Neon DB(싱가포르) ──→ 다시 워싱턴 ──→ 다시 일로일로
                    ↑ 태평양 횡단           ↑ 지구 반바퀴 더
```

페이지 하나가 쿼리를 10번 하면 그 왕복이 10번 일어난다. **Apps Script 를 버린 이유(느림)를 그대로 재현하는 셈이다.**

`sin1`(싱가포르)로 고정하면 함수와 DB가 같은 도시에 있고, 회원과도 약 2,400km 거리다.

| 구간 | 기본값(iad1) | 고정 후(sin1) |
|---|---|---|
| 함수 ↔ DB | 약 15,000km · 왕복 200ms+ | 같은 리전 · 왕복 1~5ms |
| 회원 ↔ 함수 | 약 13,000km | 약 2,400km |

## 요금제 제약

| 플랜 | 함수 리전 수 |
|---|---|
| Hobby | **단일 리전** ← 우리는 하나면 되므로 문제없음 |
| Pro | 5개 |

**플랜에서 허용하는 것보다 많은 리전을 적으면 빌드 전에 배포가 실패한다.**
그래서 `["sin1"]` 하나만 적었다. 늘리지 마라.

## 확인 방법

배포 후 Vercel 대시보드 → 해당 배포 → **Deployment Summary** 에서 리전이 `sin1` 인지 확인한다.
대시보드 **Settings → Functions → Function Regions** 에서도 볼 수 있다.

## 나중에 크론을 붙일 때

독촉·월마감·주간 무결성 검사를 자동 실행하려면 이 파일에 `crons` 를 추가한다.
**단, `/api/cron/...` 라우트를 먼저 만든 뒤에** 추가해야 한다. 없는 경로를 적으면 배포가 실패한다.
현재 `src/app/api` 자체가 없으므로 지금은 넣지 않았다.

```jsonc
{
  "regions": ["sin1"],
  "crons": [
    { "path": "/api/cron/dunning",   "schedule": "0 1 * * *" },   // 매일 09:00 마닐라 (UTC+8)
    { "path": "/api/cron/monthly",   "schedule": "0 0 1 * *" },
    { "path": "/api/cron/integrity", "schedule": "0 23 * * 0" }
  ]
}
```

> Hobby 플랜은 **크론이 하루 1회로 제한**된다. 세 개를 다 돌리려면 Pro 가 필요하거나,
> 하나의 크론이 세 작업을 순서대로 하도록 합쳐야 한다.
> 크론 라우트에는 반드시 `CRON_SECRET` 헤더 검사를 걸어라 — 없으면 누구나 독촉 메일을 트리거할 수 있다.
